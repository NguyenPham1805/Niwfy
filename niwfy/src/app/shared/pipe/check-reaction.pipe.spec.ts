import { CheckReactionPipe } from './check-reaction.pipe';

describe('CheckReactionPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckReactionPipe();
    expect(pipe).toBeTruthy();
  });
});
